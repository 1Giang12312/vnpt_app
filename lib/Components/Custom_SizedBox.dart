// ignore_for_file: file_names

import 'package:flutter/material.dart';

class TinyHeightSizedBox extends StatelessWidget {
  const TinyHeightSizedBox({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 10,
    );
  }
}

class SmallHeightSizedBox extends StatelessWidget {
  const SmallHeightSizedBox({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 20,
    );
  }
}

class MediumHeightSizedBox extends StatelessWidget {
  const MediumHeightSizedBox({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 30,
    );
  }
}

class LargeHeightSizedBox extends StatelessWidget {
  const LargeHeightSizedBox({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 40,
    );
  }
}

class HugHeightSizedBox extends StatelessWidget {
  const HugHeightSizedBox({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 65,
    );
  }
}
//width

class TinyWidthSizedBox extends StatelessWidget {
  const TinyWidthSizedBox({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 10,
    );
  }
}

class SmallWidthSizedBox extends StatelessWidget {
  const SmallWidthSizedBox({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 20,
    );
  }
}

class MediumWidthSizedBox extends StatelessWidget {
  const MediumWidthSizedBox({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 30,
    );
  }
}

class LargeWidthSizedBox extends StatelessWidget {
  const LargeWidthSizedBox({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 40,
    );
  }
}
